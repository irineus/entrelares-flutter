name: entrelares_core
description: Pure-Dart core of Entrelares — client mirrors of the server-enforced calendar and error rules, testable without an emulator.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
