/// U-27 — the shared component set. One import for the eleven pieces every
/// screen repeats, so a screen never has to decide what a badge looks like.
///
/// * [AppSectionHeader], [AppCard], [AppListRow], [AppSheetHeader] — structure
/// * [AppBanner], [AppBadge], [AppEmptyState] — state
/// * [AppTextField], [AppSegmented], [AppActionPair], [AppAvatar] — action
library;

export 'controls.dart';
export 'signals.dart';
export 'surfaces.dart';
